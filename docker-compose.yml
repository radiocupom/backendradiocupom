version: '3.9'

services:
  redis:
    image: redis:7
    container_name: backendradiocupom-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped
    command: ["redis-server", "--save", "900", "1", "--appendonly", "yes"]

volumes:
  redis-data:
